# Copy to .env and adjust for your environment.
# When running via Docker Compose these are set in docker-compose.yml directly.
# Use this file when running the API locally without Docker.

VLLM_BASE_URL=http://localhost:8001
VLLM_MODEL=mock-model
