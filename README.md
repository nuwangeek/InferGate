# InferGate
## Scalable Chat API Gateway (vLLM Backend)
### Project Overview
A production-grade chat API serving millions of users, backed by vLLM for inference. The project is built incrementally across 8 versions (v0–v7), each adding one scalability/production concern. The AI part is intentionally simple — the API layer is the focus.
